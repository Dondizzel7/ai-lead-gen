# Voiceover Script — Missed Call Text-Back Tutorial (`missed-call-demo.html`)

Voice: "Sterling" (Higgsfield seed_audio preset `dc382508-c8bd-443c-8cb2-46e57b8d2e6f`).
Scene duration = 400ms + clip + 1300ms. VO offset = scene start + 0.4s.

1. "You're under a sink, on a roof, or mid-install — and the phone rings. You can't get to it in time. That call just became a missed call. Here's what happens next, automatically."
2. "The moment that call goes unanswered, the automation fires instantly — before the caller even sets their phone down."
3. "Within seconds, they get a friendly text back, letting them know you saw the call and you're ready to help — so they never have to dial the next business on Google."
4. "When they reply, the system asks exactly what a dispatcher would: what's going on, and where. In this case, a furnace out, kids home, and no heat — flagged as urgent automatically."
5. "From there, it offers a real appointment time, locks it in, and sends a confirmation — all without a single phone call back and forth."
6. "Every missed call is tracked in one place, so you can see exactly which ones turned into real, booked jobs."
7. "If someone doesn't reply right away, a gentle follow-up goes out an hour later — and your team gets notified either way, so a real person can always step in."
8. "The results: dozens of missed calls recovered every month, real jobs booked from calls that used to just vanish, and thousands of dollars in revenue that would have gone to a competitor instead."
9. "That's Missed Call Text-Back by High-Rise Solution. Every missed call becomes a second chance. Ready to stop losing jobs to voicemail? Let's talk."

## Status: not yet generated (built after Review video hit daily VO limit)

Generate all 9 clips (seed_audio, voice Sterling dc382508-c8bd-443c-8cb2-46e57b8d2e6f) — either
individually if the daily cap allows, or via the single-long-clip strategy (see
social-script.md / review-script.md for that pattern) if it doesn't. Then follow the standard
finish: compute scene durations, render missed-call-demo.html via frames.mjs, push silent
master, Descript assemble, publish 720p, download, mux clean video+Descript audio to strip
watermark, encode missed-call-ghl-720p.mp4 + missed-call-ghl-480p-sms.mp4 +
missed-call-audio.mp3, commit to claude/ghl-workflow-demo-video-b9hp4l, SendUserFile + share
links.

This is the 4th video in the queue — build it AFTER review-automation-demo.html is fully
delivered (per user instruction: reviews first, then missed call text-back, no check-ins
needed in between — proceed straight through both).
